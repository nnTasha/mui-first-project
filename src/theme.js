import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0000FF',
      light: 'skyblue',
    },
    secondary: {
      main: '#0000FF',
    },
    otherColor: {
      main: '#00FF00',
    },
  },
});
